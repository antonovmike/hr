<odoo>
  <data>

    <!-- Actions -->
    <record id="action_view_patients" model="ir.actions.act_window">
        <field name="name">View Patients</field>
        <field name="res_model">hr_hospital.patient</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
    </record>
    <record id="action_view_doctors" model="ir.actions.act_window">
        <field name="name">View Doctors</field>
        <field name="res_model">hr_hospital.doctor</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
    </record>

    <!-- Top menu item -->
    <menuitem name="Hospital" id="menu_hr_hospital" sequence="10" />

    <!-- Sub-menu items -->
    <menuitem name="Patients" id="menu_hr_hospital_patients" parent="menu_hr_hospital" action="hr_hospital.action_view_patients" sequence="10"/>
    <menuitem name="Doctors" id="menu_hr_hospital_doctors" parent="menu_hr_hospital" action="hr_hospital.action_view_doctors" sequence="20"/>

    <!-- Representations for model Patients -->
    <record id="patient_tree" model="ir.ui.view">
        <field name="name">Patients Tree</field>
        <field name="model">hr_hospital.patient</field>
        <field name="arch" type="xml">
            <tree string="Patients">
                <field name="name"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="doctor_id"/>
            </tree>
        </field>
    </record>
    <record id="patient_form" model="ir.ui.view">
        <field name="name">Patients Form</field>
        <field name="model">hr_hospital.patient</field>
        <field name="arch" type="xml">
            <form string="Patients">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="age"/>
                        <field name="gender"/>
                        <field name="doctor_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Representations for model Doctors -->
    <record id="doctor_tree" model="ir.ui.view">
        <field name="name">Doctors Tree</field>
        <field name="model">hr_hospital.doctor</field>
        <field name="arch" type="xml">
            <tree string="Doctors">
                <field name="name"/>
                <field name="specialization"/>
            </tree>
        </field>
    </record>
    <record id="doctor_form" model="ir.ui.view">
        <field name="name">Doctors Form</field>
        <field name="model">hr_hospital.doctor</field>
        <field name="arch" type="xml">
            <form string="Doctors">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="specialization"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

  </data>
</odoo>